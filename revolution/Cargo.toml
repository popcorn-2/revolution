[package]
name = "revolution"
version = "0.1.0"
edition = "2024"

[dependencies]
proto = { path = "../../driver/proto" }
revolution_keyboard = { path = "../revolution_keyboard" }
toml = { version = "0.9.8", default-features = false, features = ["debug", "parse"] }
